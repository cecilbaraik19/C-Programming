// While Loop Program for displaying EVEN no. from 100 to 70
#include <stdio.h>
int main()
{
    int i = 100;
    while (i >= 70)
    {
        printf("%d\n", i);
        i -= 2;
    }
    return 0;
}